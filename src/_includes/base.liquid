<!DOCTYPE html>
<html lang="en">
	<head>
		<meta name="viewport" content="width=device-width,initial-scale=1.0">
		{%-if description-%}
		<meta name="description" content="{{description}}">
		{%-elsif site.description-%}
		<meta name="description" content="{{site.description}}">
		{%-endif-%}
		<title>
			{%if title%}
				{{-title-}}
			{%elsif site.name%}
				{{-site.name-}}
			{%else%}
				11ty Site
			{%endif%}

		</title>
		<link rel="stylesheet" href="{{- '/style.css' | url -}}">
	</head>
	<body class="min-h-screen bg-slate-200
		     grid gap-4 md:gap-16 mt-4 md:mt-16 mb-2 md:mb-4
		     grid-rows-[max-content_auto_min-content]">
		<header class="self-center px-4 md:px-16 grid grid-flow-col auto-cols-fr items-center">
			<h1 class="text-theme-500 text-4xl lg:text-7xl small-caps"><a href="{{ '/' | url }}">{{-site.name-}}</a></h1>
			<section class="text-sm text-right text-neutral-700
					grid place-content-end items-center
					md:grid-flow-col md:gap-2">
				<h2 class="sr-only">Jency Samuel on social media</h2>
				<a href="https://twitter.com/jencysamuel"
					rel="noreferrer" target="_blank">Twitter</a>
				<span class="not-sr-only hidden md:inline">|</span>
				<a href="https://www.instagram.com/jency.me.samuel/"
					rel="noreferrer" target="_blank">Instagram</a>
			</section>
		</header>

		{%comment%}Tailwind sets mx for <main> at layer @base{%endcomment%}
		{{-content-}}

		<footer class="text-center text-sm text-slate-800
			       grid place-content-center">
			<span>
				&copy;&nbsp;
				{{-site.author.name-}}&nbsp;2022
			</span>
		</footer>
	</body>
</html>
